/**
* test/helper.js should always be ran first when mocha --recursive is executed.
* This allows us to load the config file without having to specify through each
* test file.
**/
require('dotenv').config()
